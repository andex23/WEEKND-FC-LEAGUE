import { pageMetadata } from "@/lib/seo"
export const metadata = pageMetadata('Register for the League | Weekend FC', 'Create your Weekend FC player profile, choose your platform and club, and join our online EA FC league.', "/register")
export default function Layout({ children }: { children: React.ReactNode }) { return children }
