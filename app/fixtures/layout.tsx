import { pageMetadata } from "@/lib/seo"
export const metadata = pageMetadata('Fixtures & Matchdays | Weekend FC', 'Follow Weekend FC fixtures, scheduled matches and confirmed results from our online EA FC league.', "/fixtures")
export default function Layout({ children }: { children: React.ReactNode }) { return children }
