import { pageMetadata } from "@/lib/seo"
export const metadata = pageMetadata('League Standings | Weekend FC', 'Follow the Weekend FC league table, points, goal difference and player form throughout the season.', "/standings")
export default function Layout({ children }: { children: React.ReactNode }) { return children }
